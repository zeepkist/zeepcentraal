query ZC_SitemapLevels($after: Cursor) {
	levels(first: 1000, after: $after, orderBy: [XX_HASH_ASC]) {
		nodes {
			xxHash
			dateUpdated
			dateCreated
		}
		pageInfo {
			endCursor
			hasNextPage
		}
	}
}
