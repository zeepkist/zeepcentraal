fragment ZC_ZslLevelStandingFields on ZslLevelResult {
	position
	points
	userId
	time
	user {
		id
		steamId
		steamName
	}
	record {
		id
	}
}

query ZC_ZslLevelResults(
	$id: Int!
	$viewerId: Int!
	$includeViewer: Boolean!
	$first: Int
	$after: Cursor
	$last: Int
	$before: Cursor
) {
	zslLevelResults(
		first: $first
		after: $after
		last: $last
		before: $before
		filter: { levelId: { equalTo: $id } }
		orderBy: [POSITION_ASC]
	) {
		edges {
			cursor
			node {
				...ZC_ZslLevelStandingFields
			}
		}
		pageInfo {
			startCursor
			endCursor
			hasNextPage
			hasPreviousPage
		}
		totalCount
	}
	viewerStanding: zslLevelResults(
		first: 1
		filter: { levelId: { equalTo: $id }, userId: { equalTo: $viewerId } }
	) @include(if: $includeViewer) {
		nodes {
			...ZC_ZslLevelStandingFields
		}
	}
}
