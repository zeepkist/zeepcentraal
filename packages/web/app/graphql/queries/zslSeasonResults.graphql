query ZC_ZslSeasonResults($id: Int!, $first: Int, $after: Cursor, $last: Int, $before: Cursor) {
	zslSeasonResults(
		first: $first
		after: $after
		last: $last
		before: $before
		filter: { seasonId: { equalTo: $id } }
		orderBy: [POSITION_ASC]
	) {
		edges {
			cursor
			node {
				position
				points
				user {
					steamId
					steamName
				}
			}
		}
		pageInfo {
			startCursor
			endCursor
			hasNextPage
			hasPreviousPage
		}
		totalCount
	}
}
