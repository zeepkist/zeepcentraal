query ZC_UserResults(
	$first: Int
	$after: Cursor
	$last: Int
	$before: Cursor
	$filter: RecordFilter!
) {
	records(
		first: $first
		after: $after
		last: $last
		before: $before
		filter: $filter
		orderBy: [DATE_CREATED_DESC, ID_DESC]
	) {
		edges {
			cursor
			node {
				id
				time
				dateCreated
				levelId
				userId
				level {
					xxHash
					levelPoints {
						points
					}
					levelItems(
						first: 1
						filter: { deleted: { equalTo: false } }
						orderBy: [UPDATED_AT_DESC]
					) {
						nodes {
							name
						}
					}
				}
				userPointContributions(first: 1) {
					nodes {
						levelPoints
						playerDecayedPoints
						levelDecayedPoints
						levelPosition
						contributionRank
					}
				}
				personalBestGlobals(first: 0) {
					totalCount
				}
				worldRecordGlobals(first: 0) {
					totalCount
				}
			}
		}
		pageInfo {
			startCursor
			endCursor
			hasNextPage
			hasPreviousPage
		}
		totalCount
	}
}
