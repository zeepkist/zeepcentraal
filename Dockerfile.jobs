FROM gcr.io/distroless/base

LABEL org.opencontainers.image.description="ZeepCentraal background worker for leaderboard updates, score sync, and history jobs."

WORKDIR /app

COPY dist/zeepcentraal-jobs zeepcentraal-jobs

ENV NODE_ENV=production

CMD ["./zeepcentraal-jobs"]
