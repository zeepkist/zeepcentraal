---
name: vue-vapor-best-practices
description: MUST be used for Vue.js tasks that use Vue 3.6 Vapor Mode (beta). ALWAYS use Composition API with `<script setup vapor lang="ts">` where safe to enable Vapor Mode.
license: MIT
metadata:
  author: github.com/vuejs-ai
  version: "18.0.0"
---

# Vue Best Practices Workflow

Use this skill as an instruction set. Follow the workflow in order unless the user explicitly asks for a different order.

### 1) Must-read core references (required)

- Before implementing any Vue task, make sure to read and apply these core references:
  - `references/vue-3-6-vapor-mode.md`
- Keep these references in active working context for the entire task, not only when a specific issue appears.
