name: Deploy

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  packages: write
  pull-requests: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Bun dependencies
        uses: ./.github/actions/setup-bun-deps

      - name: Typecheck
        run: bun --bun run typecheck

      - name: Typecheck web
        run: bun --bun run typecheck:web

      - name: Run tests
        run: bun --bun run test

      - name: Run web tests
        run: bun --bun run test:web

  release:
    runs-on: ubuntu-latest
    needs: [test]
    outputs:
      version: ${{ steps.release_version.outputs.version }}
      major: ${{ steps.release_version.outputs.major }}
      major_minor: ${{ steps.release_version.outputs.major_minor }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup Bun dependencies
        uses: ./.github/actions/setup-bun-deps

      - name: Setup release Node
        uses: actions/setup-node@v7
        with:
          node-version: '26'
          package-manager-cache: false

      - name: Configure git user
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR_ID}+${GITHUB_ACTOR}@users.noreply.github.com"

      - name: Run semantic release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bun run release

      - name: Resolve release version
        id: release_version
        shell: bash
        run: |
          git fetch --force --tags origin
          VERSION="$(git tag -l --points-at "$GITHUB_SHA" --sort=-v:refname | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | head -n 1 || true)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if [ -z "$VERSION" ]; then
            echo "major=" >> "$GITHUB_OUTPUT"
            echo "major_minor=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          MAJOR="${VERSION%%.*}"
          REST="${VERSION#*.}"
          MINOR="${REST%%.*}"
          echo "major=$MAJOR" >> "$GITHUB_OUTPUT"
          echo "major_minor=$MAJOR.$MINOR" >> "$GITHUB_OUTPUT"

  build-packages:
    runs-on: ubuntu-latest
    needs: [test, release]
    strategy:
      fail-fast: false
      matrix:
        package:
          - name: server
            build_script: build:server
            artifact_path: dist/zeepcentraal-server
            include_hidden_files: false
          - name: jobs
            build_script: build:jobs
            artifact_path: dist/zeepcentraal-jobs
            include_hidden_files: false
          - name: migrate
            build_script: build:migrate
            artifact_path: dist/zeepcentraal-migrate
            include_hidden_files: false
          - name: import-zsl
            build_script: build:import-zsl
            artifact_path: dist/zeepcentraal-import-zsl
            include_hidden_files: false
          - name: postgraphile
            build_script: build:postgraphile
            artifact_path: dist/zeepcentraal-postgraphile
            include_hidden_files: false
          - name: web
            build_script: build:web
            artifact_path: packages/web/.output/
            include_hidden_files: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}

      - name: Build ${{ matrix.package.name }} output
        uses: ./.github/actions/build-package-output
        with:
          name: ${{ matrix.package.name }}
          build-script: ${{ matrix.package.build_script }}
          artifact-path: ${{ matrix.package.artifact_path }}
          include-hidden-files: ${{ matrix.package.include_hidden_files }}

  docker-build:
    runs-on: ubuntu-latest
    needs: [build-packages, release]
    strategy:
      fail-fast: true
      matrix:
        image:
          - name: server
            file: Dockerfile.server
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-server
            image_name: server
            clone_super_league: false
          - name: jobs
            file: Dockerfile.jobs
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-jobs
            image_name: jobs
            clone_super_league: false
          - name: migrate
            file: Dockerfile.migrate
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-migrate
            image_name: migrate
            clone_super_league: false
          - name: import-zsl
            file: Dockerfile.zsl
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-import-zsl
            image_name: import-zsl
            clone_super_league: true
          - name: postgraphile
            file: Dockerfile.postgraphile
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-postgraphile
            image_name: postgraphile
            clone_super_league: false
          - name: web
            file: Dockerfile.web
            artifact_download_path: packages/web/.output
            expected_path: packages/web/.output/server/index.mjs
            image_name: web
            clone_super_league: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}

      - name: Build ${{ matrix.image.name }} image (no push)
        uses: ./.github/actions/build-docker-image
        with:
          name: ${{ matrix.image.name }}
          dockerfile: ${{ matrix.image.file }}
          artifact-download-path: ${{ matrix.image.artifact_download_path }}
          expected-path: ${{ matrix.image.expected_path }}
          clone-super-league: ${{ matrix.image.clone_super_league }}
          service-version: ${{ needs.release.outputs.version || github.sha }}
          push: false
          tags: ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:${{ github.sha }}

  docker-publish:
    runs-on: ubuntu-latest
    needs: [docker-build, release]
    if: needs.release.outputs.version != ''
    strategy:
      fail-fast: true
      matrix:
        image:
          - name: server
            file: Dockerfile.server
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-server
            image_name: server
            clone_super_league: false
          - name: jobs
            file: Dockerfile.jobs
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-jobs
            image_name: jobs
            clone_super_league: false
          - name: migrate
            file: Dockerfile.migrate
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-migrate
            image_name: migrate
            clone_super_league: false
          - name: import-zsl
            file: Dockerfile.zsl
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-import-zsl
            image_name: import-zsl
            clone_super_league: true
          - name: postgraphile
            file: Dockerfile.postgraphile
            artifact_download_path: dist
            expected_path: dist/zeepcentraal-postgraphile
            image_name: postgraphile
            clone_super_league: false
          - name: web
            file: Dockerfile.web
            artifact_download_path: packages/web/.output
            expected_path: packages/web/.output/server/index.mjs
            image_name: web
            clone_super_league: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish ${{ matrix.image.name }} image
        uses: ./.github/actions/build-docker-image
        with:
          name: ${{ matrix.image.name }}
          dockerfile: ${{ matrix.image.file }}
          artifact-download-path: ${{ matrix.image.artifact_download_path }}
          expected-path: ${{ matrix.image.expected_path }}
          clone-super-league: ${{ matrix.image.clone_super_league }}
          service-version: ${{ needs.release.outputs.version }}
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:latest
            ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:${{ github.sha }}
            ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:${{ needs.release.outputs.major }}
            ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:${{ needs.release.outputs.major_minor }}
            ghcr.io/${{ github.repository }}/${{ matrix.image.image_name }}:${{ needs.release.outputs.version }}
